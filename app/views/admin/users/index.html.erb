 <div class="container">
<table class="table table-striped">
  <thead>
    <tr>
      <th>id</th>
      <th>名前</th>
      <th>email</th>
      <th>製作日</th>
      <th>タスク数</th>
      <th>管理者</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |admin| %>
      <tr>
        <td><%= admin.id %></td>
        <td><%= admin.user_name %></td>
        <td><%= admin.email %></td>
        <td><%= admin.created_at.strftime("%Y-%m-%d %H:%M") %></td>
        <td><%= admin.tasks.count %></td>
        <td><%=  "私は管理者" if admin.admin == true %></td>
        
        
        <td></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>